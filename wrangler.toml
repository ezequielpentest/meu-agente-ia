[build]
  command = "npm run build"
  destination = "dist"

[build.environment]
  NODE_VERSION = "20"

# As variáveis de ambiente com dados sensíveis (como ANTHROPIC_API_KEY)
# são configuradas no painel do Cloudflare, NÃO aqui neste arquivo.
# Este arquivo vai para o repositório — nunca coloque chaves aqui.
